import React from "react";
import a from "./ProfileInfo.module.css";

const ProfileInfo = (props) => {
    return (
        <div className={a.descriptions}> ava + descriptions</div>
    )
};
export default ProfileInfo;